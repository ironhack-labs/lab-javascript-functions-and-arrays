// Find the maximum
function maxOfTwoNumbers(a,b)
{
 if (a > b){
   return a;
  }else {
  return b;
  }
};

//Find longest word
 function findLongestWord(words) {
 var words = [
  'foo','bar'
];
var longest=0;
var result;
for (i = 0; i < words; i++) {
  if (words[i].length > longest) 
   var longest = words[i].length;
   var result = words[i];
   } 
  }

  //calculating a sum

  function sumArray (sum){
    var array = [0,1,3];
    var sum = [];

    for (var i = 0; i < array,length; i++){ 
    sum += array[i];
    
    
      
}}

//uniquify array
function uniquifyArray(words) {
  if (words.length > 0) {
    var wordsUnique = [];

    for (var i = 0; i < words.length; i++) {
      if (words.indexOf(words[i], i+1) == -1) {
        wordsUnique.push(words[i]);
      }
    }

    return wordsUnique;
    return uniquifyArray;
  }
}

var words = ['crab','poison','contagious','simple'];
    
    
